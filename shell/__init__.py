"""The imperative shell: everything that touches the world.

``shell/`` may import ``core/``; the reverse never happens. Database, geocoder,
language model, PDF writer, HTTP and the clock all live here.
"""
