---
topic: astro-report v1 — single-operator tool turning five client facts into a grounded eight-section Italian monthly astrological report
updated: 2026-08-28T23:08
---

- (event) create operation: no prior spec folder at _bmad-output/specs/spec-astro-report; first derive
- (decision) slug inherited as astro-report from the upstream artifact folders (prd-astro-report-2026-08-14 et al), not re-derived
- (decision) input is the full upstream chain: brief.md + brief addendum, prd.md + prd addendum, ARCHITECTURE-SPINE.md + BUILD-ORDER.md, product_research.md; treated as structured and pre-sorted, so authored separation is trusted
- (decision) express distillation chosen over guided: input is rich (1036-line PRD with 30 FRs, 471-line architecture spine with 19 invariants), so gaps become open_questions rather than elicitation
- (decision) prd.md adopted as a companion rather than listed as a source, because its FR-level testable consequences remain the acceptance detail downstream needs and reproducing them in the kernel would duplicate 1000 lines
- (decision) ARCHITECTURE-SPINE.md and BUILD-ORDER.md adopted as companions; the spine owns AD-1..AD-19, stack, conventions and diagrams, so the kernel cites them and never restates them
- (decision) brief.md, brief addendum, prd addendum and product_research.md listed as sources: fully absorbed, and each contains superseded technical claims (SQLite-on-disk, Groq failover, Python 3.11, four-date sampling) that the architecture overrode — adopting them as contract would hand downstream stale rules
- (decision) spec-authored companion computation-tables.md created to hold the live tuning data the brief addendum was the sole authority for (traditional/modern Ruler tables, orb ranges, aspect set, house system, body sets, harmonic/disharmonic table, detection definitions); it is declared authoritative where a source restates a value, matching AD-18's single-owner rule
- (decision) spec-authored companion sections.md created as the per-Section catalog: fixed order, Payload material per FR-13, and interpretive territory recovered from prd addendum section 8 — the primary raw material for the Style Guide (CAP-16)
- (decision) no spec-authored glossary: PRD section 3 and the spine's Consistency Conventions already carry the vocabulary verbatim, and a third copy would drift; the kernel carries the discipline rule instead
- (decision) external companion and source paths written project-root-relative, matching the convention the sibling architecture spine already uses in this project; spec-authored companions are bare filenames
- (decision) ReportTheme derivation not raised to a capability: it is the mechanism behind CAP-19 and Spec Law rule 2 keeps implementation prescription in a companion (spine AD-14)
- (decision) FR-15 folded into CAP-25 and FR-17 folded into CAP-18: each pair is one reviewable surface, so splitting them would produce capabilities with no independent success criterion
- (decision) two capabilities have no FR behind them and are carried from the architecture: CAP-27 operator logical backup (AD-17, without which the non-negotiable durability NFR is unmet) and CAP-29 Astro.com conformance harness (PRD section 9.1 in-scope item and the mechanism behind SM-3)
- (capability) CAP-1 create a Client from the five inputs, complete or not at all (FR-1)
- (capability) CAP-2 resolve a free-text birthplace to coordinates and the UTC offset in force at the birth instant (FR-2)
- (capability) CAP-3 compute and store the Natal Chart once per Client (FR-3)
- (capability) CAP-4 correct birth data, recomputing the chart while retaining prior Reports against the superseded one (FR-4)
- (capability) CAP-5 render the chart wheel for operator verification only (FR-5)
- (capability) CAP-6 resolve traditional and modern Rulers for all twelve cusps (FR-6)
- (capability) CAP-7 assemble the four Domain Profiles by pure derivation (FR-7)
- (capability) CAP-8 scan the requested month continuously and date every Transit Event exactly (FR-8)
- (capability) CAP-9 detect transit-to-natal Aspects and locate their perfection instants (FR-9)
- (capability) CAP-10 identify retrograde bodies and date every Station (FR-10)
- (capability) CAP-11 detect Ingresses across natal house cusps including retrograde re-crossings (FR-11)
- (capability) CAP-12 locate the month's Lunations with degree and natal house (FR-12)
- (capability) CAP-13 assemble the Report Payload per Section as a pure function (FR-13)
- (capability) CAP-14 version and persist the Report Payload immutably alongside its Report (FR-14)
- (capability) CAP-15 generate the eight Sections in Italian in fixed order (FR-16)
- (capability) CAP-16 author and version the Style Guide, and condition every generation on it (FR-30, FR-17)
- (capability) CAP-17 write a recurring Client's Report as a continuation rather than a restatement (FR-18)
- (capability) CAP-18 absorb transient generation failures without operator involvement (FR-19)
- (capability) CAP-19 verify every Claim against the Report Payload before the operator sees the Report (FR-20)
- (capability) CAP-20 regenerate on Gate failure and surface persistent failure with the contradicting Payload entries (FR-21)
- (capability) CAP-21 retain and report Gate outcomes across Reports (FR-22)
- (capability) CAP-22 collect the Corpus and report its paired/unpaired composition (FR-23, FR-24)
- (capability) CAP-23 restrict the application to a single principal (FR-28)
- (capability) CAP-24 delete a Client and everything derived from them (FR-29)
- (capability) CAP-25 review a Report with its Report Payload and Gate result alongside (FR-25, FR-15)
- (capability) CAP-26 export a passed Report to PDF and Markdown, capturing send disposition and elapsed time (FR-26)
- (capability) CAP-27 produce an operator-held logical backup with a visible staleness signal (spine AD-17)
- (capability) CAP-28 browse Report History for a Client (FR-27)
- (capability) CAP-29 verify computed output against Astro.com reference charts (PRD section 9.1, SM-3)
- (constraint) the Generator narrates and never computes: it receives only the Report Payload, the Style Guide version and the two ReportThemes, with no tools, no database handle and no prior Report prose (PRD 6.1, AD-3)
- (constraint) no astronomical fact may appear in a Report that is not in its Report Payload, and the Groundedness Gate is the only path to export (PRD 4.6, AD-7)
- (constraint) claim-level determinism is the bar, not byte-identical prose: wording may vary between runs, Claims may never vary, exceed the Payload or contradict each other
- (constraint) identical Client birth data, month and ComputationConfig produce a byte-identical Report Payload on every run and every deployment
- (constraint) Reports ship unedited under Francesco's professional name; every guardrail must hold without a human catching the failure
- (constraint) output language is Italian under every configuration
- (constraint) exact birth time to the minute is mandatory; no noon chart, solar houses or house-less path exists anywhere in the system
- (constraint) Placidus houses, the five major aspects only, natal Orb default 7.0 degrees (tunable 6.0-8.0), transit-to-natal Orb default 2.0 degrees (tunable 1.5-2.5), transiting Moon excluded from Aspects and entering only through Lunations
- (constraint) every astronomical tuning value lives in one versioned ComputationConfig, passed explicitly and recorded with its hash on every Payload; changing the harmonic/disharmonic rule is a data edit and version bump, never a code change (AD-18)
- (constraint) all computation and storage is UTC; the analyzed month is one half-open UTC interval derived from the Client's local calendar month, so every Transit Event belongs to exactly one Report (AD-12)
- (constraint) the ephemeris files are vendored, pinned by SHA-256 and asserted at boot; the Moshier fallback is never an accepted runtime state (AD-2)
- (constraint) all durable state lives in Postgres; nothing written to the compute host's filesystem at runtime is ever read back after a restart (AD-11)
- (constraint) running cost stays at zero at 30-200 Reports per month; a design requiring paid infrastructure at target volume must be raised rather than absorbed
- (constraint) hosting and storage are EU/EEA; exactly one Generator adapter is configured with no runtime failover, and its data terms are verified before real Client data is sent and again on any provider change (PRD 6.2, AD-9)
- (constraint) exactly one principal, enforced structurally: no users table, no invitation flow, no password reset; adding a second principal is a PRD revision and would also trigger the AGPL source-offer obligation (AD-15)
- (constraint) Reports are non-fatalistic: no fixed outcome, medical event, death or financial result is ever predicted
- (constraint) the dated entries of Sections 6 and 7 are projected from the Payload by code; a date token written by the Generator inside those Sections is a Gate violation (AD-5)
- (constraint) the PRD section 3 glossary is the vocabulary verbatim across code, database and configuration, and the four domains stay Italian and lowercase; introducing a synonym is a defect
- (constraint) no Report reaches a Client before Astro.com conformance passes across the full reference set
- (constraint) losing a Report Payload permanently breaks the traceability guarantee for its Report and is not acceptable; loss of a Natal Chart is recoverable by recomputation
- (constraint) one Report goes from Client selected to Report on screen in under 3 minutes at p90, and total operator involvement per Report stays under 15 minutes
- (assumption) the harmonic/disharmonic classification rule is conventional Western practice inferred rather than supplied by Francesco, particularly the treatment of conjunctions by transiting body; carried into computation-tables.md pending his confirmation (PRD Assumption 1, Open Question 4)
- (assumption) Sections 6 and 7 may use list form while Sections 1-5 and 8 must be continuous prose (PRD Assumption 2)
- (assumption) the 3-minute p90 latency budget and the 10-second full-month scan bound are derived from the throughput target, not measured; E11 measures them and loosens the documented numbers if reality disagrees (PRD Assumptions 3 and 4)
- (assumption) the chart wheel adequately replaces the four-date snapshot as Francesco's verification habit (PRD Assumption 5)
- (assumption) availability is best-effort with no SLA, inferred from the batch working pattern (PRD Assumption 6)
- (assumption) Report delivery to Clients stays manual and out of scope (PRD Assumption 7)
- (assumption) prd addendum section 3 states a data processing agreement is required before real Client data is entered, while PRD section 6.2 consciously defers the DPA as accepted risk; the PRD is the later decision of record and is taken to supersede, but the divergence is surfaced rather than silently resolved
- (question) where does the Groundedness Gate draw the line for an interpretive statement that leans on a fact without naming it? AD-8 answers it structurally with a closed vocabulary and states the limit explicitly, but the empirical calibration against real Reports is still open (PRD Open Question 1)
- (question) is the harmonic/disharmonic classification rule correct? Needs Francesco's professional confirmation; getting it wrong misfiles days between Giorni favorevoli and Giorni di attenzione, the most client-visible error the product can make (PRD Open Question 4)
- (question) what is the anonymization position for Corpus content? Required before phase-2 conditioning, not before v1, but it shapes how the Corpus is collected now (PRD Open Question 3)
- (question) does the Benessere Section produce GDPR Article 9 special category data? Deferred at PRD 6.2; if it qualifies, the lawful basis and safeguards change materially
- (question) PRD Open Question 2 (how the preceding Report is summarized for FR-18) is answered by spine AD-14 — derive_theme is pure and model-free — so it is closed here rather than carried forward
- (event) self-validate pass 1 coherence: all 29 capabilities carry both intent and success; intents state WHAT with implementation left to the adopted spine; every constraint rules something out; 13 non-goals stated; success signal is demonstrable via SM-1, SM-2 and SM-3. PASS
- (event) self-validate pass 2 preservation: walked prd.md FR-1..FR-30, NFRs, sections 6-11 and section 15; ARCHITECTURE-SPINE.md AD-1..AD-19, conventions, stack and deferred list; BUILD-ORDER.md E0..E11; brief.md and both addenda; product_research.md. Every load-bearing claim lands in SPEC.md, computation-tables.md, sections.md, or an adopted companion. PASS
- (event) wrapper-only content dropped: review-rubric.md, reconcile-product-research.md and reviews/review-finalize-gate.md are process records of how upstream artifacts were produced and how their findings were applied; their findings are already in the PRD and spine, so they are neither companions nor sources
- (event) wrapper-only content dropped: UJ-1/UJ-2/UJ-3 narrative framing, competitive positioning against Co-Star/Astro.com/generic AI, and the hour-arithmetic backstory are compressed into the Why paragraph rather than preserved verbatim; brief.md retains them as a source
- (event) spec finalized: SPEC.md, computation-tables.md and sections.md derived from this memlog
- (decision) path convention sharpened in frontmatter: a companions entry with no directory part is a spec-authored sibling; every other path, adopted companion or source, resolves from the project root — which is what disambiguates the root-level product_research.md
- (decision) Francesco confirms the harmonic/disharmonic classification rule is correct as stated, including the treatment of conjunctions by transiting body; it is now confirmed domain fact rather than inference, closing PRD Open Question 4 and PRD Assumption 1
- (decision) Francesco determines the Benessere Section does not produce GDPR Article 9 special category data; closes the position deferred at PRD section 6.2, and with it the last data-protection question the spec carried
- (note) the Article 9 determination does not relax the register: PRD section 6.1's ban on predicting medical events stands independently, so the sections.md caution on Benessere prose remains in force on product-safety grounds alone
- (decision) the AD-18 rule that the classification table is a data edit and a version bump rather than a code change is retained unchanged; confirmation removes the pending-answer rationale but the single-owner requirement never rested on it
- (event) re-derive after Francesco's answers: SPEC.md drops one assumption and two open questions, leaving two open; computation-tables.md marks the classification table confirmed; sections.md drops the GDPR limb of the Benessere caution
- (event) upstream divergence flagged, not edited: prd.md (FR-13 assumption tag, section 6.2 deferred Article 9 item, Open Question 4, Assumptions Index 1), prd addendum section 8 caution, ARCHITECTURE-SPINE.md AD-5/AD-18 binds plus the deferred list, and BUILD-ORDER.md now reference resolved items. prd.md and the spine are adopted companions owned by their originating skills, so bmad-spec does not edit them; updating them is offered to Francesco
- (event) upstream divergence CLOSED: prd.md, prd addendum.md, ARCHITECTURE-SPINE.md and BUILD-ORDER.md all synced on 2026-08-14 via bmad-prd and bmad-architecture update runs. All twelve stale references resolved; PRD Assumption 1 kept at its number and AD-1..AD-19 unchanged, so every cross-reference in SPEC.md and its companions still resolves. No re-derive of SPEC.md needed - the spec never quoted the stale text
- (event) update operation 2026-08-28: two upstream changes since the 2026-08-14 derive -- bmad-ux produced EXPERIENCE.md + DESIGN.md (ux-astro-report-2026-08-28, both status final), and a bmad-architecture update added AD-20 to the spine. Resumed from this memlog; CAP-1..CAP-29 preserved; next free id CAP-30.
- (decision) EXPERIENCE.md and DESIGN.md adopted as companions, project-root-relative paths under _bmad-output/planning-artifacts/ux-designs/ux-astro-report-2026-08-28/. They are the load-bearing operator-UI contract downstream must read (information architecture, state/interaction patterns, key flows, WCAG 2.1 AA floor; visual identity #42297A-on-white, Inter, light+dark tokens). bmad-ux owns them; bmad-spec references, never edits.
- (decision) ARCHITECTURE-SPINE.md (already an adopted companion) now carries AD-1..AD-20. AD-20: a ReportRun advances one stage per poll GET of /report-runs/<run_id>; the start POST creates the row and returns immediately without driving; concurrent polls single-flighted by a Postgres transaction-scoped advisory lock on the run id; no thread, task, queue or cron ever advances a run. No kernel restatement -- the spine owns the mechanism -- but CAP-30 and BUILD-ORDER E5 depend on it.
- (capability) CAP-30 watch a report run progress: Francesco starts a run and watches it move through its six stages, and can leave the view or close the tab and return without the run being lost or restarted. success: starting a run returns to the run view immediately without waiting for any stage (spine AD-20); the view shows the current stage and reflects each advance; a run left or closed mid-flight resumes from its last completed stage when the view is reopened (spine AD-10); a terminally failed run shows which stage failed and why -- distinct from a Gate failure, which routes to the CAP-20 review surface. Mechanism in spine AD-10/AD-20 and EXPERIENCE.md 'Report Run Lifecycle'.
- (constraint) the operator-facing web UI conforms to the adopted EXPERIENCE.md (information architecture, voice, component/state/interaction patterns, key flows, WCAG 2.1 AA floor) and DESIGN.md (visual identity: #42297A on white, Inter, light and dark themes). These companions are binding, not reference.
- (decision) existing constraint 'output language is Italian under every configuration' broadened: Italian spans BOTH report content (unchanged, CAP-15) AND the entire operator UI -- chrome, navigation, labels, helper text, errors, empty states, toasts, stage labels, and the native date/time pickers (document lang=it; displayed dates dd/MM/yyyy, times HH:mm). The UI half is new from the ux run; identifiers (YYYY-MM month codes, hashes, UUIDs) stay latin-alphanumeric as mono chips.
- (note) spine AD-20's non-blocking poll-driven advance does not threaten the 'under 3 minutes at p90' latency constraint: the draft_ready stage (the Gemini call) dominates wall-clock under either driving model and the budget keeps margin; release validation (BUILD-ORDER E11) measures it regardless.
- (event) self-validate pass 1 coherence: CAP-30 carries both intent and success; its intent states WHAT (watch a run, leave/return) with the HOW left to cited spine AD-10/AD-20 and EXPERIENCE.md (rule 2). New constraints rule things out -- 'UI conforms to EXPERIENCE.md + DESIGN.md, binding' bars an ad-hoc UI; the broadened Italian line bars English chrome (rule 3). Non-goals still explicit (rule 4). Success signal unchanged and concrete (rule 5). CAP-30 is the next unused id, CAP-1..CAP-29 untouched (rule 6). New prose is load-bearing, no decoration (rule 8). PASS
- (event) self-validate pass 2 preservation: walked EXPERIENCE.md and DESIGN.md claim by claim. Italian-only UI -> broadened Italian constraint. IA / sidebar / state patterns / key flows / WCAG 2.1 AA -> 'operator UI conforms to EXPERIENCE.md' constraint (binding companion). Report Run Lifecycle -> CAP-30. Non-blocking start + poll-driven advance -> CAP-30 success + no-worker non-goal + AD-20 citation. DESIGN.md visual identity -> 'conforms to DESIGN.md' constraint. Design-token detail, hex ramp, component specs and the six flows correctly stay in the binding companions, not restated in the kernel (rule 2). AD-20 mechanism stays in the spine (now AD-1..AD-20). Nothing dropped silently. PASS
- (event) computation-tables.md and sections.md unchanged this run -- no memlog entry affects them. stories.yaml not present; Story Breakdown not run (update scope is companion adoption + one capability, no new independently-shippable slices beyond the existing epics.md).
- (event) spec re-derived: SPEC.md gains CAP-30, two constraints (UI conformance + broadened Italian), one non-goal clause (no background worker), and EXPERIENCE.md + DESIGN.md in companions. 30 capabilities. computation-tables.md and sections.md byte-unchanged.
